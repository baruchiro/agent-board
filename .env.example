# Port the server listens on inside the container.
PORT=4100

# SQLite file. In Docker this lives on the agent-board-data volume.
DB_PATH=/data/agent-board.sqlite

# Characters of prompt text / shell command kept before truncation (§10.4).
# The full value is never stored unless the reporting host has retain_raw=1.
REDACT_KEEP=200
